apiVersion: gpu.gpu.nvidia.com/v1alpha1
kind: GPUHealth
metadata:
  labels:
    app.kubernetes.io/name: nvidia
    app.kubernetes.io/managed-by: kustomize
  name: gpuhealth-sample
spec:
  # TODO(user): Add fields here
